import { Box } from "@chakra-ui/react";
import Header from "./Header";

type LayoutProps = {};

const Layout = () => {
  return (
    <Box>
      <Header></Header>
    </Box>
  );
};

export default Layout;